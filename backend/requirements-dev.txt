-r requirements.txt
pytest
requests
# scripts/throughput_model.py only (master doc v6.0 Section 15) — a one-off
# deck deliverable, not runtime code, so these stay out of requirements.txt.
simpy
matplotlib
