# CLAUDE.md

Read this at the start of every session. It is the shared reference for both developers on SilicaGuard. If anything here conflicts with the master reference document (`SilicaGuardcompletedocumentation_final.docx`, v4.0, FINAL), the reference document wins — flag the conflict and update this file.

## Project identity

SilicaGuard is an occupational lung health screening system for Zimbabwe's artisanal and small-scale mining workforce — miners covered by neither NSSA nor any existing occupational health programme (formal/enterprise mining employers already run their own; see "Current sprint status" for the 5 August scope pivot). A health worker takes an Android app to a mine site, delivers a fifteen-minute group education session using illustrated cards on the phone, then screens each miner in about ten minutes with no doctor present. The system classifies each worker into one of four risk tiers, compares the result against their previous screening to detect deterioration early, and tells each miner the single most important thing he personally should change — drawn from his own answers. Where a referral is needed it routes it to the right facility with a deadline, pre-alerts that facility, and chases the worker until attendance is confirmed. Miners who miss the outreach day self-screen by dialling a USSD code from any phone, with no internet and no cost. Everyone receives their result and reminders by SMS. Hospitals see every referral and whether it was completed. Silicosis is the first module; tuberculosis is next, on the same infrastructure.

- **Prototype presentation**: 11 August 2026 (online demonstration preferred).
- **Feature freeze**: end of Day 8 of the sprint. Days 9–11 are testing, rehearsal and evidence only — no new functionality.
- **Sprint window**: 1–11 August 2026. Note: the sprint plan's "Day 1" is dated 1 August; if today's date is later than that, count forward from 1 August to find the actual current day rather than assuming Day 1.

## Ownership boundaries

Two developers share this repo. Respect these boundaries — **never edit a file in the other person's directory without saying so first.**

| Area | Owner |
|---|---|
| `backend/` (schema, AI services, all API routes, deployment, seed scripts) | Panashe — AI & Backend Engineer |
| `mobile/` (React Native / Expo app) | Collaborator — Mobile & UX/UI Engineer |
| `dashboard/` (React web dashboard) | Collaborator — Mobile & UX/UI Engineer |
| `docs/`, `CLAUDE.md`, the API contract, `.env.example`, database schema | **Shared — requires agreement before changing** |

Any change to the database schema or an API response shape must be announced in the shared progress log before merging — it breaks the other developer's work if it lands silently.

## Tech stack — do not suggest alternatives

- **Backend**: Python FastAPI, SQLite (MVP; migrates to PostgreSQL at pilot scale), deployed on Render free tier.
- **Mobile**: React Native (Expo), `expo-sqlite`, offline-first. No Flutter.
- **Dashboard**: React + Vite + Recharts + Leaflet, deployed on Vercel.
- **AI**: Google Gemini 2.5 Flash via the Gemini API. Claude API is the documented drop-in alternative if Anthropic billing becomes available — that's why `ANTHROPIC_API_KEY` exists in `.env.example` even though it's currently unused.
- **USSD and SMS**: Africa's Talking, called directly over `httpx` — **not** the official SDK, which fails with an SSL error on Windows in this environment.
- **No WhatsApp. No voice/IVR. No Flutter. No chest X-ray model.** These were built or considered in earlier versions (v1.0–v3.0) and were deliberately removed. Do not reintroduce them.

## The four AI modules — and nothing else is AI

1. **Risk Stratification Engine** — screening answers → four-tier classification, reasoning like an occupational health physician, output includes the personalised advice line.
2. **Longitudinal Deterioration Detection** — compares a re-screen against the worker's previous screening; any adverse trajectory escalates the tier one level even if absolute values wouldn't warrant it.
3. **Smart Referral Router** — sets urgency window, matches facility, composes the referral packet, schedules reminders/escalation.
4. **Population Health Intelligence** — weekly job producing site-clustering analysis and a plain-language narrative for the hospital dashboard.

The mobile app UI, the dashboards, SMS dispatch, Teach Mode cards, the USSD decision tree, the outreach planner and the compliance export are **not AI**. They are good software engineering. Never describe them as AI — overclaiming weakens the four real claims.

## Non-negotiable rules

Copied verbatim from the reference document's implementation map. These are not up for reinterpretation in the course of normal feature work — if one seems wrong, say so and raise it rather than quietly deviating.

- Hard red-flag overrides live in Python code, outside the model call. The model can never downgrade a RED.
- USSD handlers must respond within 10 seconds and complete a session within 180 seconds. No LLM call inside a USSD session.
- Every screening stores the AI input and output together, so any classification can be audited afterwards.
- The mobile app must complete a full screening with no connectivity and sync cleanly afterwards. Test in airplane mode before declaring done.
- Phone numbers are hashed at rest. Never store a raw phone number in an exportable table.
- Every patient-facing string requires the Clinical Lead's written sign-off before it ships.
- Every screening result must carry a personalised advice line. A result without one is incomplete.

## Coding conventions

- API responses use `snake_case` JSON keys.
- All timestamps stored in UTC, ISO 8601.
- Never hardcode secrets — always read from environment variables.
- Any change to the database schema or an API response shape must be announced in the shared log before merging.

## Git workflow

- `main` is protected and always deployable.
- Branch naming: `be/<short-description>` for backend, `fe/<short-description>` for frontend.
- Small commits, clear messages, pull request into `main`.
- Pull from `main` before starting work every day.

## Current sprint status

Last updated: 10 August 2026 (sprint Day 10, presentation Day 11 — tomorrow). **Note on this section's own history**: PR #2 (scope pivot) and PR #3 (risk-engine hardening) were developed in parallel branches that both edited this section independently; when both merged to `main`, PR #2's version of this section won and PR #3's updates (safety overrides/deterioration/advice-line landing) were silently lost from this file — the code itself was never lost, only this status writeup. Rewritten below to reflect everything that's actually true as of now, all in one place.

**All of the following is merged to `main`:**

- **Phase A schema migration** (PR #1, 2 August): four-tier `tier` column (`GREEN`/`YELLOW`/`ORANGE`/`RED`, CHECK-constrained), `previous_screening_id`, `advice_line`, `provisional` columns added; `xray_results`/`whatsapp_messages` tables dropped; `referrals` status lifecycle now `open → pre_alerted → reminded → attended → closed → escalated` with `deadline` set on creation (RED = 48h, ORANGE = 14 days).
- **Artisanal-only scope pivot** (PR #2, 5 August): per Dr Bopoto's feedback, formal/enterprise mining companies already run their own pneumoconiosis programmes, so SilicaGuard targets artisanal miners only now. Removed the Enterprise Occupational Health pillar entirely — `employers`/`campaigns` tables dropped (not left dead), the target `employer` role and its privacy-boundary rule, both `/api/enterprise/*` target routes. Full detail and the "who pays" rewrite are in `SILICAGUARD.md` Section 13. **The `.docx` master reference document still has not been manually updated** — still describes the enterprise pillar; see that session's handoff checklist for exact sections to change.
- **Risk Stratification Engine hardening** (PR #3, 5 August): **hard red-flag safety overrides now live in Python** (`backend/services/safety_overrides.py`), closing the non-negotiable-rule violation — applied after the AI call, so the model can never downgrade a RED. **Longitudinal Deterioration Detection is built** (`backend/services/deterioration.py`) — escalates tier one level on any worsened symptom/exposure answer vs the worker's previous screening. **Personalised advice generation is built** (`backend/services/advice_engine.py`) — `advice_line` is now always populated, template-bound (not model-generated free text), selected from the miner's single weakest answer. **Draft copy, not yet Clinical Lead signed off.**
- **Smart Referral Router + Population Health Intelligence** (branch `be/smart-referral-router-and-population-intel`, 5 August, today):
  - Worker endpoints rebuilt as `POST /api/workers` + new `GET /api/workers/{phone}` (replaces the old `POST /api/miners`, which now 404s). Unauthenticated, same deliberate precedent as `/api/screen`.
  - **Facility matching is live** (`backend/services/facility_matching.py`): RED always matches a `district_hospital`-level facility; ORANGE tries a `clinic` whose name plausibly serves the worker's site, falling back to the hospital. Replaces the old hardcoded `'Kwekwe District Hospital'` literal.
  - **Reminder/escalation cascade is live** (`backend/services/referral_cascade.py`): RED gets one reminder at ~24h, escalates at 48h; ORANGE reminds at day 3 and day 7, escalates at day 14. Fires via an in-process **APScheduler** job wired into `main.py`'s lifespan (`SCHEDULER_INTERVAL_MINUTES`, default 10 min; `SCHEDULER_ENABLED=false` disables it — set automatically under pytest). Only runs while the server process is awake.
  - **Population Health Intelligence is live** (`backend/services/population_intelligence.py`) — `GET /api/dashboard/week`'s `ai_narrative` is now a real Gemini call (generated fresh per request, not cached/scheduled — a fast-follow candidate), with a deterministic fallback string if the call fails. New `tier_distribution` field on the same response.
  - Schema: `referrals.facility_id`, `referrals.reminder_stage` added via `ALTER TABLE` (wrapped for idempotency) — unlike most schema changes in this project, an existing local/deployed DB does **not** need deleting for this one.
  - `docs/API_CONTRACT.md` fully updated to match all of the above.
  - All four AI modules are now substantively built for the first time this sprint.
- **Outreach Planner + SMS audit trail** (branch `be/outreach-planner-and-notifications`, 6 August, today):
  - **Outreach Planner is live**: `POST`/`GET /api/outreach` (`backend/services/outreach.py`, `backend/routers/outreach.py`), both requiring auth. Schedule a visit; a second APScheduler job on the same instance as the referral cascade sends the 3-day/1-day bulk SMS announcement to every worker at that site and flips `report_generated` once the visit date passes. `POST /api/screen` gained an optional `outreach_visit_id` (explicit-if-valid, else inferred for `channel: "APP"` screenings by site+date match) — `screened_count` increments live, and `GET /api/outreach` computes `tier_distribution`/`referral_list` live from every linked screening. **Known simplification**: "report ready" = "the scheduled date has passed," since there's no real mobile-sync-completion signal in the schema.
  - **SMS audit trail is live**: new `notifications` table — every one of the five SMS-sending functions in `backend/services/notifications.py` now logs `worker_id`/`template`/`payload`/`delivery_status` (`sent`/`failed`/`skipped`) per send, not just a server log line. No endpoint reads it yet (audit-only for now).
  - Schema: `screenings.outreach_visit_id`, `outreach_visits.sms_3day_sent`/`sms_1day_sent` added via `ALTER TABLE`; new `notifications` table.
  - `docs/API_CONTRACT.md` fully updated to match.
- **Deployed to Render** (6 August, same day): **live at `https://silicaguard-backend.onrender.com`**. Storage decision confirmed: free tier has no persistent disk, so `AUTO_SEED_ON_BOOT=true` reseeds the demo dataset on every boot rather than paying for a disk or migrating to Postgres this close to feature freeze. Created via Render's REST API directly (not the dashboard Blueprint flow, which had trouble — turned out `render.yaml` was missing an explicit `plan: free`, defaulting new services to the paid `starter` tier; fixed in the file regardless so a future Blueprint sync stays correct, even though this deploy bypassed it). Full endpoint sweep confirmed live: health, login, dashboard (proving the reseed worked), referrals, outreach, worker registration.
- **First real mobile↔backend end-to-end test, and post-mortem fixes** (7 August, today): the collaborator's mobile app (`feat/mobile-frontend` branch, via Expo Go tunnel) was used to screen a real test miner against the **live deployed Render backend**, confirmed by direct DB checks — worker registration, `POST /api/screen`, the Python safety override, `advice_line`, and an auto-created facility-matched referral with a correct 48h RED deadline all worked end to end for real. The mobile app's earlier direct-Claude-call bug and question-code drift (both flagged in an earlier session) are confirmed already fixed on that branch (commit `2973811`). Two backend gaps found from that test were fixed this session:
  - **`explanation_shona` is now live on `/api/screen`** (`backend/services/explanation_shona.py`, `ScreeningResult.explanation_shona`) — previously absent entirely, so the mobile app always rendered a hardcoded filler string regardless of tier or content. Template-bound, same mechanism and same weakest-answer selection as `advice_line`, persisted into the existing `screenings.ai_explanation_shona` column. **Draft copy, not yet Clinical Lead signed off**, same as `advice_line`.
  - **Result SMS now fires for all four tiers**, not just ORANGE/RED (`backend/services/notifications.send_screening_result_sms`, `backend/services/tier_messages.py`) — closes a real gap against this doc's own "Everyone receives their result... by SMS" claim; previously GREEN/YELLOW miners saw their result once, live, and got nothing afterward (APP path) or nothing at all (USSD path). `tier_messages.py` also fixed a real correctness bug as a side effect: the APP path previously sent every ORANGE/RED referral the same ORANGE-flavored Shona text (`GENERIC_REFERRAL_SHONA_MESSAGE`, now removed) even for RED; RED now gets RED-specific wording, matching what USSD already did.
  - Full pytest suite: 131 passing (was 118), plus a live smoke test against a local server confirming both changes end to end (including a real Africa's Talking sandbox SMS send logged to `notifications`).
  - `docs/API_CONTRACT.md` updated to match both changes.
  - Several mobile-side (`mobile/`, collaborator's directory) bugs were found but **not fixed here** per ownership rules — ready-to-paste patches written up instead, not applied: a navigation crash from three unregistered routes (the debug toast visible in every screenshot of that test), a decorative "referral card" screen that fabricates a hospital-notified confirmation with zero network call, and — most importantly — a repeat-screening path that silently and permanently falls back to offline scoring for any previously-registered miner (defeating Longitudinal Deterioration Detection for return visits), because `GET /api/workers/{phone}` was never wired in even though it's been live since 5 August. **Note**: the pushed branch's `api.js` also still posts worker registration to the dead `/api/miners` route with the wrong field name (`mine_site` instead of `site`) — yet the live test registered correctly, meaning the collaborator's actual working tree already has unpushed local fixes beyond this branch. He needs to push before these patches are applied, to avoid the two of you colliding.
- **Mines dropdown + live VHW dashboard numbers** (branch `be/shona-mines-dashboard-live-testing`, 7 August, today, same session, not yet pushed):
  - **New `mines` table + `GET`/`POST /api/mines`** (`backend/routers/mines.py`) — a curated Midlands-province mine list (11 seeded, `backend/scripts/seed_demo_data.py`) for the VHW's outreach-site dropdown, replacing the hardcoded `'Globe & Phoenix Mine'` default baked into the mobile app's intake screen. Unauthenticated, same field-worker precedent as `POST /api/workers`. Deliberately **not** a foreign key target — `miners.mine_site`/`outreach_visits.site` both stay free `TEXT`; a full migration tying them to `mines.id` was judged too risky this close to feature freeze, so this is a suggestion list for the UI, not a hard constraint yet.
  - **New `GET /api/dashboard/today`** (`backend/routers/dashboard.py`, optional `?site=` filter) — powers the mobile Home screen's real numbers (previously all hardcoded to 0): `screened_today`, `todays_log` (today's screenings), `refer_now` (open/pre_alerted/reminded/escalated referrals — drops an entry once it's `attended`/`closed`, i.e. a live "have they taken action" worklist), `watch` (miners whose *most recent* screening is YELLOW). Unauthenticated, same tradeoff already accepted for `GET /api/workers/{phone}` — returns names/phone numbers/tiers without a login, needed so a field VHW can actually call someone back.
  - 10 new tests (141 total, was 131), plus a live smoke test confirming both endpoints against freshly-seeded demo data (`refer_now`/`watch` counts and the site filter all verified correct).
  - `docs/API_CONTRACT.md` updated to match; `mobile-patches.md` (this session's scratchpad) updated with how to wire both into the Home screen and intake dropdown.
  - **Real SMS/USSD live-testing guidance written up, not executed here** — registering a real phone as an Africa's Talking sandbox Simulator Number (no code change, doable immediately) works for real SMS; a real phone dialling a real USSD code needs an AT Live-app upgrade + a provisioned USSD channel, both account-level/KYC actions on Africa's Talking's own approval timeline, outside this repo and outside what I can do directly — full steps and an honest fallback (AT's sandbox web simulator) are in this session's `live-testing-setup.md` scratchpad note.
  - **Merged to `main`** 7 August (PR #9), same day.
- **Mobile PR #8 merged** (7 August): the collaborator's own fix for the `/api/miners`→`/api/workers` and `mine_site`→`site` bugs flagged above, plus wiring `GET /api/workers/{phone}` into the 409-fallback path — confirmed both live against the deployed backend before merging (`POST /api/workers` request/response shape, and the exact `"already registered"` 409 detail string his client-side fallback matches on). Squash-merged directly (his network was down, couldn't merge it himself) after his explicit go-ahead.
- **Mobile follow-up fixes** (branch `fe/home-live-data-nav-mines-fix`, PR #10, opened as a **draft** 7 August, still open): built on top of PR #8 with the user's explicit authorization to cross into `mobile/` given the collaborator's network outage and the freeze deadline — nav crash (three Home-screen buttons pointed at unregistered routes, the "action NAVIGATE not handled" toast in every screenshot), Home screen's stat cards wired to real `GET /api/dashboard/today` data with tap-through to a new `WorklistScreen` (Refer Now/Watch/Today's Log, tap-to-call), a `MineSitePicker` dropdown backed by `GET /api/mines` replacing the hardcoded intake default, and the referral card's fabricated "Hospital Notified" claim softened to describe the real automatic server-side pre-alert instead of asserting a live confirmation it never checked. **Deliberately left as a draft PR, not merged** — no way to verify a mobile UI change without a browser/device in this environment; needs the collaborator's or the user's own on-device confirmation first.
- **Intelligence Dashboard built** (branch `fe/intelligent-dashboard`, 10 August, today) — `dashboard/` had zero code anywhere before today (confirmed via `git log --all -- dashboard/`); built the night before the 11 August demo once the user named it as one of six things they need to present. **Zero backend changes** — one static HTML/CSS/JS page (`dashboard/index.html`, `style.css`, `app.js`, no build step, a flagged deviation from the documented React+Vite+Recharts+Leaflet stack — see `SILICAGUARD.md` Section 5's note), fetching the live deployed backend directly (`CORS allow_origins=["*"]` already permits it). One unified view, not four role-scoped logins — a login screen (reusing `POST /api/auth/login`'s existing `hospital` account), headline stats + AI narrative + tier distribution + site breakdown from `GET /api/dashboard/week`, a live referral queue from `GET /api/referrals` with working Mark Attended/Closed buttons (`PATCH /api/referrals/{id}`, already-tested endpoint), and an Outreach Planner panel from `GET /api/outreach`. Verified without a browser: static cross-checks (every DOM id/class the JS references exists in the HTML/CSS — none missing), a Node `--check` syntax pass, and a DOM-stub harness that actually executed every render function against real captured live API responses plus empty-data edge cases with no runtime errors. **Still needs a real look in an actual browser before presenting it** — this verification proves it doesn't crash and reads the right fields, not that it's visually correct.
- `SILICAGUARD.md` updated same session to match reality: Teach Mode and the QR referral card split out as explicitly not built (both were previously bundled into rows that implied they existed); the dashboard's actual delivered form (static HTML, one view) documented against the target React stack; Section 14's Demo Day Scenarios rewritten to match what's actually being demoed tomorrow.

**Phone-number hashing rule, reviewed not changed**: `CLAUDE.md`'s non-negotiable rule says phone numbers must be hashed at rest and never stored raw in an exportable table. `miners.phone` stays plaintext — it's the operational identity for SMS delivery, USSD caller lookup, and `GET /api/workers/{phone}`; a one-way hash there breaks all three (you cannot text a hash). The rule's actual target, an *exportable* table, doesn't exist post-pivot — the enterprise CSV export that would have been that surface is out of scope. Recommend treating the rule as binding on any future bulk-export surface specifically, not the operational table. Flagging per the rule's own "if one seems wrong, say so" instruction, not deciding unilaterally — raise with the team if this reasoning doesn't hold up.

**Still not started / still open going into Day 11**: Teach Mode and the QR referral card (both genuinely not built — see `SILICAGUARD.md` Section 4, don't claim either live), Clinical Lead sign-off on all draft copy (advice-line templates, `explanation_shona` templates, every SMS template, the ORANGE Shona/English copy, the narrative prompt), auth role rename (declined, no remaining case needs it post-pivot), the phone-number hashing decision needs the team's sign-off. USSD self-screening has never been tested over Africa's Talking's actual carrier network — sandbox and the local simulator only; no production shortcode provisioned, realistically won't be before tomorrow — demo via Africa's Talking's own sandbox web simulator, framed honestly as simulated, not live-carrier. **PR #10** (mobile nav/dashboard/mines fixes) is still an open draft, unmerged, unverified on a real device. **The new `dashboard/`** needs an actual look in a real browser before presenting it — everything checked here was static/headless verification. **Decide before the demo**: whether to merge PR #10 and `fe/intelligent-dashboard` beforehand or present straight off their branches; which exact mobile build (main vs. PR #10) gets shown; re-seed demo data fresh and warm the Render instance (30–50s cold start, confirmed again today) ahead of time.

Update this section at the end of each session.
